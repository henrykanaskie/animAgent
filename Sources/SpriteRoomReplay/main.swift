// Scaffold only. M1 turns this into the replay harness that drives fixtures/
// through the WorldModel and prints the resulting deltas.

import SpriteRoomCore

print("spriteroom-replay \(SpriteRoomCore.version) — not yet implemented")
