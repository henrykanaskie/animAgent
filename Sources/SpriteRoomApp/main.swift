// Scaffold only. M2 hosts the scene in a plain window; M3 replaces that with
// the notch panel. See docs/05-MILESTONES.md.

import SpriteRoomCore

print("spriteroom \(SpriteRoomCore.version) — not yet implemented")
